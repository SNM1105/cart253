/**
 * Function Calls
 * Simon Medalssy
 * 
 * Working on function calls examples.
 */

"use strict";


function setup() {
    //once at the beginning of ther program. Within the brackets are arguments thats define the width and height if the canvas.
    createCanvas(640, 480);
}

function draw() {
    //every frame. Within parentheses are arguments that specify colour
    background(255, 100, 100);
}